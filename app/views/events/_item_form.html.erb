<%= form_for [@event, @event.items.build], remote: true, :html => { :class => 'form-horizontal' } do |f| %>
<tr>
	<td><%= f.collection_select :person_id, Person.all, :id, :name, {:prompt => true} %></td>
	<td><%= f.submit 'Gravar', :class => 'btn btn-primary'%></td>
	<td></td>
</tr>
<% end %>